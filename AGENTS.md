# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

Este proyecto usa **SDK 56** a propósito (no el último), porque es el que soporta
la app **Expo Go** de las tiendas. Ver CLAUDE.md.
