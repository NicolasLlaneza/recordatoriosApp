# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Este proyecto usa **SDK 54** a propósito (no el último), porque es el que soporta
la app **Expo Go** instalada en el teléfono del usuario. Ver CLAUDE.md.
